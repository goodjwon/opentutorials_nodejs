doctype html
html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') ServerSideScrpit
        hr
        ul
            each topic in topics
                li
                    a(href='/topic/' + topic.id)=topic.title
        article
           h1= 'DELETE? ' + topic.title
           form(action='/topic/'+topic.id+'/delete' method='post')
                p
                    input(type='submit')